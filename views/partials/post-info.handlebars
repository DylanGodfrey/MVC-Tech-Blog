<div class="card">
    <div class="card-header">
        <h4><a href="/post/{{id}}">{{title}}</a></h4>
        <h6>Posted by {{user.username}} on {{created_at}}</h6>
    </div>
    <div>
        <p id="custom-content">{{content}}</p>
    </div>
</div>